@model IEnumerable<Proyecto_Fase2.Models.CxC60>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_User)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateLimit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Id_LatePayment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalAmount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_User)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateLimit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Id_LatePayment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalAmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
